name: deploy to staging
on: 
  push: 
    branches: 
     - main 

jobs: 
 
  deploy:
    name: deloying on staging ec2 cluster
    runs-on: ubuntu-latest
    steps:

    - name: performing action manually 
      run: |
        ssh -i "${{secrets.EC2_INSTANCE_KEY}}" ubuntu@3.110.194.13
        cd ChatApp
        pm2 kill
        npm install 
        pm2 start server.js --name chatapp

  

   